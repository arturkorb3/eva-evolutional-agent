# GitHub Pages config. Wraps the rendered README in a layout that also renders
# Mermaid diagrams (GitHub Pages/Jekyll does not render Mermaid by default).
title: "EVA — Evolutional Agent"
defaults:
  - scope:
      path: ""
    values:
      layout: default
